import { FormatRutDirective } from './format-rut.directive';

describe('FormatRutDirective', () => {
  it('should create an instance', () => {
    // const directive = new FormatRutDirective();
    // expect(directive).toBeTruthy();
  });
});
