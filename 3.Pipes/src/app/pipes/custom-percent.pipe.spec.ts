import { CustomPercentPipe } from './custom-percent.pipe';

describe('CustomPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
